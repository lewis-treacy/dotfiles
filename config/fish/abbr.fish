abbr -a ls "ls -lh --color=auto"
abbr -a la "ls -A"
abbr -a mkdir "mkdir -p"
abbr -a ya "yaourt"
abbr -a yas "yaourt -Syua"

abbr -a v vim
abbr -a vi vim
abbr -a wim vim
abbr -a vim nvim

abbr -a sysinfo "neofetch; hex"

abbr -a msf "msfconsole -x \"db_connect lewis@msf\""
